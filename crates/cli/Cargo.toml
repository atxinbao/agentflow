[package]
name = "agentflow-cli"
edition.workspace = true
license.workspace = true
version.workspace = true

[[bin]]
name = "agentflow"
path = "src/main.rs"

[dependencies]
agentflow-core = { path = "../core" }
agentflow-agent-bridge = { path = "../agent-bridge" }
agentflow-event-store = { path = "../event-store" }
agentflow-execute = { path = "../execute" }
agentflow-input = { path = "../input" }
agentflow-loop = { path = "../loop" }
agentflow-projection = { path = "../projection" }
agentflow-release = { path = "../release" }
agentflow-state = { path = "../state" }
agentflow-task-loop = { path = "../task-loop" }
agentflow-workflow-events = { path = "../workflow-events" }
anyhow.workspace = true
clap.workspace = true
serde_json.workspace = true

[dev-dependencies]
agentflow-mcp = { path = "../mcp" }
agentflow-spec = { path = "../spec" }
tempfile.workspace = true
