name: release-gate

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"
  release:
    types:
      - published
  workflow_dispatch:

jobs:
  release-gate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libglib2.0-dev \
            pkg-config \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Install desktop dependencies
        run: npm --prefix apps/desktop ci

      - name: Resolve release metadata
        id: release_meta
        run: |
          event_name="${GITHUB_EVENT_NAME}"
          release_tag=""
          release_url=""

          if [[ "$event_name" == "push" && "${GITHUB_REF_TYPE:-}" == "tag" ]]; then
            release_tag="${GITHUB_REF_NAME}"
          elif [[ "$event_name" == "release" ]]; then
            release_tag="$(python3 -c 'import json, os; from pathlib import Path; payload = json.loads(Path(os.environ["GITHUB_EVENT_PATH"]).read_text(encoding="utf-8")); print((payload.get("release") or {}).get("tag_name", ""))')"
            release_url="$(python3 -c 'import json, os; from pathlib import Path; payload = json.loads(Path(os.environ["GITHUB_EVENT_PATH"]).read_text(encoding="utf-8")); print((payload.get("release") or {}).get("html_url", ""))')"
          fi

          if [[ -z "$release_tag" ]]; then
            package_version="$(python3 -c 'import tomllib; from pathlib import Path; payload = tomllib.loads(Path("Cargo.toml").read_text(encoding="utf-8")); print(payload["workspace"]["package"]["version"])')"
            release_tag="v${package_version}"
          fi
          if [[ -z "$release_url" ]]; then
            release_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/tag/${release_tag}"
          fi

          artifact_dir="artifacts/release-gate-${release_tag}-e2e"
          certification_dir="artifacts/release-gate-${release_tag}-certification"
          echo "release_tag=$release_tag" >> "$GITHUB_OUTPUT"
          echo "release_version=$release_tag" >> "$GITHUB_OUTPUT"
          echo "release_url=$release_url" >> "$GITHUB_OUTPUT"
          echo "artifact_dir=$artifact_dir" >> "$GITHUB_OUTPUT"
          echo "certification_dir=$certification_dir" >> "$GITHUB_OUTPUT"

      - name: Rust format check
        run: cargo fmt --all --check

      - name: Rust tests
        env:
          RUST_TEST_THREADS: 1
        run: cargo test --workspace

      - name: Desktop build
        run: npm --prefix apps/desktop run build

      - name: Browser preview smoke
        run: |
          artifact_dir="${{ steps.release_meta.outputs.artifact_dir }}"
          mkdir -p "$artifact_dir"
          log_path="$artifact_dir/browser-preview-smoke.log"
          status_path="$artifact_dir/browser-preview-smoke.status.json"

          set +e
          npm --prefix apps/desktop run preview:smoke > "$log_path" 2>&1
          exit_code=$?
          set -e

          node -e "const fs=require('fs'); const code=Number(process.argv[2]); fs.writeFileSync(process.argv[1], JSON.stringify({command:'npm --prefix apps/desktop run preview:smoke', exitCode: code, status: code === 0 ? 'passed' : 'failed', logPath:'browser-preview-smoke.log'}, null, 2) + '\n');" "$status_path" "$exit_code"

          cat "$log_path"
          exit "$exit_code"

      - name: Console readiness
        run: |
          artifact_dir="${{ steps.release_meta.outputs.artifact_dir }}"
          mkdir -p "$artifact_dir"
          log_path="$artifact_dir/console-readiness.log"
          status_path="$artifact_dir/console-readiness.status.json"

          set +e
          npm --prefix apps/desktop run console:readiness > "$log_path" 2>&1
          exit_code=$?
          set -e

          node -e "const fs=require('fs'); const code=Number(process.argv[2]); fs.writeFileSync(process.argv[1], JSON.stringify({command:'npm --prefix apps/desktop run console:readiness', exitCode: code, status: code === 0 ? 'passed' : 'failed', logPath:'console-readiness.log'}, null, 2) + '\n');" "$status_path" "$exit_code"

          cat "$log_path"
          exit "$exit_code"

      - name: Release gate E2E
        env:
          GITHUB_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ steps.release_meta.outputs.release_version }}
          RELEASE_TAG_NAME: ${{ steps.release_meta.outputs.release_tag }}
          RELEASE_URL: ${{ steps.release_meta.outputs.release_url }}
          SOURCE_COMMIT_SHA: ${{ github.sha }}
        run: |
          bash scripts/verify_release_gate.sh \
            --artifact-dir "${{ steps.release_meta.outputs.artifact_dir }}" \
            --release-version "${{ steps.release_meta.outputs.release_version }}" \
            --release-tag "${{ steps.release_meta.outputs.release_tag }}" \
            --source-commit-sha "${{ github.sha }}" \
            --release-url "${{ steps.release_meta.outputs.release_url }}"

      - name: Append gate summary
        if: always()
        run: |
          artifact_dir="${{ steps.release_meta.outputs.artifact_dir }}"
          if [ -f "$artifact_dir/certification.md" ]; then
            cat "$artifact_dir/certification.md" >> "$GITHUB_STEP_SUMMARY"
            printf "\n\n" >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f "$artifact_dir/summary.md" ]; then
            cat "$artifact_dir/summary.md" >> "$GITHUB_STEP_SUMMARY"
          fi
          for status_file in \
            "$artifact_dir/browser-preview-smoke.status.json" \
            "$artifact_dir/console-readiness.status.json"
          do
            if [ -f "$status_file" ]; then
              printf "\n\n## %s\n\n" "$(basename "$status_file" .status.json)" >> "$GITHUB_STEP_SUMMARY"
              printf '```json\n' >> "$GITHUB_STEP_SUMMARY"
              cat "$status_file" >> "$GITHUB_STEP_SUMMARY"
              printf '```\n' >> "$GITHUB_STEP_SUMMARY"
            fi
          done

      - name: Prepare certification artifact
        if: always()
        run: |
          artifact_dir="${{ steps.release_meta.outputs.artifact_dir }}"
          certification_dir="${{ steps.release_meta.outputs.certification_dir }}"
          rm -rf "$certification_dir"
          mkdir -p "$certification_dir/runtime"

          for file in \
            status.json \
            summary.json \
            summary.md \
            certification.json \
            certification.md \
            artifact-manifest.json \
            stage-log.jsonl \
            browser-preview-smoke.status.json \
            console-readiness.status.json
          do
            if [ -f "$artifact_dir/$file" ]; then
              cp "$artifact_dir/$file" "$certification_dir/$file"
            fi
          done

          if [ -d "$artifact_dir/runtime" ]; then
            cp -R "$artifact_dir/runtime/." "$certification_dir/runtime/"
          fi

          for file in \
            pack-projection-readiness.json
          do
            if [ -f "$artifact_dir/$file" ]; then
              cp "$artifact_dir/$file" "$certification_dir/$file"
            fi
          done

          primary_proof_list="${RUNNER_TEMP:-/tmp}/release-gate-primary-proofs.txt"
          python3 - "$artifact_dir" "$certification_dir" "$primary_proof_list" <<'PY'
          import json
          import pathlib
          import shutil
          import sys

          artifact_dir = pathlib.Path(sys.argv[1])
          certification_dir = pathlib.Path(sys.argv[2])
          output_path = pathlib.Path(sys.argv[3])
          source_root = pathlib.Path.cwd()
          certification_path = certification_dir / "certification.json"
          paths = []
          if certification_path.is_file():
              payload = json.loads(certification_path.read_text(encoding="utf-8"))
              paths = [path for path in payload.get("primaryProofs", []) if isinstance(path, str) and path]
          if not paths:
              runtime_dir = certification_dir / "runtime"
              if runtime_dir.is_dir():
                  paths = sorted(
                      path.relative_to(certification_dir).as_posix()
                      for path in runtime_dir.iterdir()
                      if path.is_file()
                  )

          for relative in paths:
              artifact_path = artifact_dir / relative
              quick_path = certification_dir / relative
              source_path = source_root / relative

              if not artifact_path.is_file() and source_path.is_file():
                  artifact_path.parent.mkdir(parents=True, exist_ok=True)
                  shutil.copy2(source_path, artifact_path)
              if not quick_path.is_file() and artifact_path.is_file():
                  quick_path.parent.mkdir(parents=True, exist_ok=True)
                  shutil.copy2(artifact_path, quick_path)

          output_path.write_text("\n".join(paths) + ("\n" if paths else ""), encoding="utf-8")
          PY

          check_primary_proofs() {
            local root="$1"
            local primary_proof
            if [ ! -s "$primary_proof_list" ]; then
              echo "missing certification primary proof list" >&2
              return 1
            fi
            while IFS= read -r primary_proof; do
              if [ -z "$primary_proof" ]; then
                continue
              fi
              if [ ! -f "$root/$primary_proof" ]; then
                echo "missing certification primary proof: $primary_proof" >&2
                return 1
              fi
            done < "$primary_proof_list"
          }

          check_primary_proofs "$certification_dir"

          python3 - "$artifact_dir" "$certification_dir" "$primary_proof_list" <<'PY'
          import hashlib
          import json
          import pathlib
          import sys
          import time

          artifact_dir = pathlib.Path(sys.argv[1])
          certification_dir = pathlib.Path(sys.argv[2])
          primary_proof_list = pathlib.Path(sys.argv[3])
          manifest_path = certification_dir / "quick-audit-manifest.json"
          required_primary_proofs = [
              line.strip()
              for line in primary_proof_list.read_text(encoding="utf-8").splitlines()
              if line.strip()
          ]

          def file_meta(path):
              return {
                  "sha256": hashlib.sha256(path.read_bytes()).hexdigest(),
                  "bytes": path.stat().st_size,
              }

          full_paths = {
              path.relative_to(artifact_dir).as_posix()
              for path in artifact_dir.rglob("*")
              if path.is_file()
          }
          quick_paths = {
              path.relative_to(certification_dir).as_posix()
              for path in certification_dir.rglob("*")
              if path.is_file() and path.name != manifest_path.name
          }
          all_paths = sorted(full_paths | quick_paths | set(required_primary_proofs))

          entries = []
          errors = []
          for relative in all_paths:
              full_path = artifact_dir / relative
              quick_path = certification_dir / relative
              full_exists = full_path.is_file()
              quick_exists = quick_path.is_file()
              included = quick_exists
              entry = {
                  "path": relative,
                  "fullArtifactExists": full_exists,
                  "certificationArtifactExists": quick_exists,
                  "includedInQuickAuditPackage": included,
                  "requiredPrimaryProof": relative in required_primary_proofs,
              }
              if full_exists:
                  entry["fullArtifact"] = file_meta(full_path)
              if quick_exists:
                  entry["quickAuditArtifact"] = file_meta(quick_path)
              if relative in required_primary_proofs and not (full_exists and quick_exists and included):
                  errors.append(f"required primary proof not truthfully included: {relative}")
              if entry["certificationArtifactExists"] != quick_exists:
                  errors.append(f"certificationArtifactExists drifted from filesystem truth: {relative}")
              if entry["includedInQuickAuditPackage"] != quick_exists:
                  errors.append(f"includedInQuickAuditPackage drifted from filesystem truth: {relative}")
              entries.append(entry)

          entry_by_path = {entry["path"]: entry for entry in entries}
          for relative in sorted(quick_paths):
              entry = entry_by_path.get(relative)
              if not entry or not entry["includedInQuickAuditPackage"]:
                  errors.append(f"quick package file missing from truthful manifest: {relative}")

          payload = {
              "version": "agentflow-release-certification-quick-audit-manifest.v1",
              "status": "failed" if errors else "passed",
              "artifactRoot": str(artifact_dir),
              "certificationRoot": str(certification_dir),
              "requiredPrimaryProofs": required_primary_proofs,
              "entries": entries,
              "failedTruthfulness": errors,
              "checkedAt": int(time.time()),
          }
          manifest_path.write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
          if errors:
              raise SystemExit("; ".join(errors))
          PY

          python3 - "$certification_dir/quick-audit-manifest.json" "$certification_dir" <<'PY'
          import json
          import pathlib
          import sys

          manifest_path = pathlib.Path(sys.argv[1])
          certification_dir = pathlib.Path(sys.argv[2])
          payload = json.loads(manifest_path.read_text(encoding="utf-8"))
          errors = []
          for entry in payload.get("entries", []):
              relative = entry.get("path")
              quick_path = certification_dir / relative
              quick_exists = quick_path.is_file()
              if entry.get("certificationArtifactExists") is True and not quick_exists:
                  errors.append(f"manifest claims missing certification artifact exists: {relative}")
              if entry.get("includedInQuickAuditPackage") is True and not quick_exists:
                  errors.append(f"manifest claims missing quick-audit artifact is included: {relative}")
              if quick_exists and entry.get("includedInQuickAuditPackage") is not True:
                  errors.append(f"manifest omits included quick-audit artifact: {relative}")
          if payload.get("status") != "passed":
              errors.append("quick-audit manifest status is not passed")
          if errors:
              raise SystemExit("; ".join(errors))
          PY

          negative_dir="${RUNNER_TEMP:-/tmp}/release-gate-certification-primary-proof-negative"
          rm -rf "$negative_dir"
          cp -R "$certification_dir" "$negative_dir"
          negative_primary_proof="$(head -n 1 "$primary_proof_list")"
          if [ -z "$negative_primary_proof" ]; then
            echo "primary proof negative check failed: primary proof list is empty" >&2
            exit 1
          fi
          rm -f "$negative_dir/$negative_primary_proof"
          if check_primary_proofs "$negative_dir" >/tmp/release-gate-certification-primary-proof-negative.log 2>&1; then
            echo "primary proof negative check failed: missing required proof was accepted: $negative_primary_proof" >&2
            exit 1
          else
            cat /tmp/release-gate-certification-primary-proof-negative.log
            echo "primary proof negative check passed"
          fi

          if python3 - "$negative_dir/quick-audit-manifest.json" "$negative_dir" >/tmp/release-gate-certification-manifest-negative.log 2>&1 <<'PY'
          import json
          import pathlib
          import sys

          manifest_path = pathlib.Path(sys.argv[1])
          certification_dir = pathlib.Path(sys.argv[2])
          payload = json.loads(manifest_path.read_text(encoding="utf-8"))
          errors = []
          for entry in payload.get("entries", []):
              relative = entry.get("path")
              quick_path = certification_dir / relative
              if entry.get("certificationArtifactExists") is True and not quick_path.is_file():
                  errors.append(f"manifest claims missing certification artifact exists: {relative}")
              if entry.get("includedInQuickAuditPackage") is True and not quick_path.is_file():
                  errors.append(f"manifest claims missing quick-audit artifact is included: {relative}")
          if errors:
              raise SystemExit("; ".join(errors))
          PY
          then
            echo "quick-audit manifest negative check failed: misleading exists=true state was accepted" >&2
            exit 1
          else
            cat /tmp/release-gate-certification-manifest-negative.log
            echo "quick-audit manifest negative check passed"
          fi

      - name: Upload certification artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-gate-certification-${{ steps.release_meta.outputs.release_version }}
          path: ${{ steps.release_meta.outputs.certification_dir }}
          if-no-files-found: error

      - name: Upload full gate artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-gate-full-${{ steps.release_meta.outputs.release_version }}
          path: ${{ steps.release_meta.outputs.artifact_dir }}
          if-no-files-found: warn
